{
  "name": "gothamwebdev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:e2e": "playwright test --headed",
    "test:smoke:refresh": "rm -f playwright-artifacts/smoke/*.png && playwright test tests/smoke.spec.js --headed",
    "dev": "eleventy --serve",
    "dev:gh-pages": "SITE_BASE=/gothamwebdev/ eleventy --serve",
    "build": "eleventy",
    "build:root": "SITE_BASE=/ eleventy",
    "build:gh-pages": "SITE_BASE=/gothamwebdev/ eleventy",
    "clean": "rm -rf _site",
    "new-post": "node scripts/new-post.js",
    "publish-post": "node scripts/publish-post.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@playwright/test": "^1.58.2"
  }
}
